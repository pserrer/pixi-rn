<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Driving the vibrator directly needs this; merged into the host app. -->
  <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
